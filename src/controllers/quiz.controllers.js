const db = require('../database/database');

// Fonction récupérer un quiz

// Fonction créer un quiz

// Fonction supression d'article

//ajout module
module.exports = {
	get **,
	post **,
	delete **,
};